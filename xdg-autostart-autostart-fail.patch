--- xdg-autostart-0.1/libxdgutils/XDGAutostart.cpp~	2011-02-11 04:50:07.000000000 -0500
+++ xdg-autostart-0.1/libxdgutils/XDGAutostart.cpp	2013-12-18 09:19:17.612153936 -0500
@@ -66,7 +66,7 @@ void XDGAutostart::run(const char *envir
 	autostartDir = opendir(it->c_str());
 	if (!autostartDir) {
 	    std::cerr << "Error opening " << *it << " directory\n";
-	    return;
+	    continue;
 	}
 
 	while ((dirEntry = readdir(autostartDir))) {
